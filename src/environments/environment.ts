import { Environment } from "@core/models/environment.model";

/**
 * Production environment datas
 */
export const environment: Environment = {
    production: false,
    apiUrl: "https://localhost:50001/api/v1",
};
